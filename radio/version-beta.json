{ "version": "1.17.33", "url": "https://mayflowerstudios.net/radio/beta/MayflowerRadio.exe", "notes": "Beta: fixes \"this station hasn't sent its song list yet\" — the list was only ever sent once at startup, so a single failed attempt stuck until the next restart, and said nothing. It now retries until it lands, skips a song whose tags won't read instead of taking the whole list down with it, and tells you if the database refuses it." }
