{ "version": "1.17.9", "url": "https://mayflowerstudios.net/radio/beta/MayflowerRadio.exe", "notes": "Beta: the app now checks for updates every half hour while running (was every few hours), so new versions show up sooner. Also includes: jingles avoid back-to-back repeats, single-instance guard, per-station stats, and safer updates." }
