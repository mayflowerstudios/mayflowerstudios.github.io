{ "version": "1.17.8", "url": "https://mayflowerstudios.net/radio/beta/MayflowerRadio.exe", "notes": "Beta: jingles won't play the same one twice in a row when you have more than one. Also: a second launch surfaces the running copy instead of starting a rival, each station keeps its own listening stats, updates roll back safely if a swap fails, and the main window's listener count is tidier." }
